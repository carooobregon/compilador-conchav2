programa miPrimerPrograma;
var caro, maria, luis: entero;

funcion cadena newCadena(cadena a, entero o, flotante b)
var m,t : entero;
{
   t = m + 4;
   devolver 4;
};

funcion cadena myfunc(entero oo, entero lalal)
var w,t : entero;
{
   escribir("hola profe");
   t = 2 + 5;
   devolver m;
};

principal(){
   newCadena("gaba", 3 + 3, 3.0/2.0);
   caro = 5 + 4 + 5;
};