programa factorial;
var num, ans : entero;


funcion entero patito(entero num){
    devolver num * 3;
};

funcion entero pelos(entero param){
    escribir(param);
    devolver patito(param * 2);
};

funcion entero factorial(entero n)
var i: entero; {
    si(n == 1){
        devolver n;
    };
    devolver n * factorial(n - 1);
};

principal(){

    escribir(pelos(2));
};