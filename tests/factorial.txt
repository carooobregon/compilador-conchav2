programa factorial;
var fac, i, num : entero;

principal(){
    escribir("Ingresa un numero");
    num = leer();
    fac = 1;
    i = 1;
    mientras(i - 1 < num ){
        fac = fac * i;
        i = i + 1;
    }
    escribir("Factorial:", fac );
};