programa miPrimerPrograma;
var caro, gaba, concha : entero;

funcion cadena newCadena(cadena a, entero b, flotante b)
var m,t : entero;
{
   concha = 3;
   devolver 4;
};

funcion cadena myfunc(entero oo, entero lalal)
var m,t : entero;
{
   t = 2;
   devolver m;
};

principal(){
   newCadena("gaba", 3 + 3, 3.0/2.0);
   caro = 5 + 4;
};