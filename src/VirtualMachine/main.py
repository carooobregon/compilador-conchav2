# Main.py can be used to test the virtual machine independently of the parsing process 

from virtualmachine import VirtualMachine

def main():
    vm = VirtualMachine()
    vm.runVM()

if __name__=='__main__':
    main()# Stack example