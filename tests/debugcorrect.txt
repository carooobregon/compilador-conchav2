programa miPrimerPrograma;
var caro, gaba, concha : entero;

funcion cadena newCadena(cadena a, entero b)
var m,t : entero;
{
   concha = 3;
   devolver 4;
};

funcion cadena myfunc(entero oo, entero lalal)
var m,t : entero;
{
   oo = 5 + 3;
  devolver m;
};

principal(){
   newCadena("gaba", 3 + 3);
   myfunc(gaba, caro);
   caro = 5 + 4;
};